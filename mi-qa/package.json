{
  "name": "hospital-qa",
  "version": "1.0.0",
  "description": "Suite de pruebas de carga y rendimiento para PharmaCRM — Hospital",
  "private": true,
  "scripts": {
    "test:smoke": "artillery run tests/basico.yml --output reports/smoke-report.json",
    "test:api": "artillery run tests/api-rest.yml --output reports/api-report.json",
    "test:scenarios": "artillery run tests/escenarios.yml --output reports/scenarios-report.json",
    "test:load": "artillery run tests/carga.yml --output reports/load-report.json",
    "test:stress": "artillery run tests/avanzado.yml --output reports/stress-report.json",
    "test:all": "npm run test:smoke && npm run test:api && npm run test:scenarios",
    "report": "artillery report reports/smoke-report.json && artillery report reports/api-report.json"
  },
  "devDependencies": {
    "artillery": "^2.0.31"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

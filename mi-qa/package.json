{
  "name": "hospital-qa",
  "version": "1.0.0",
  "description": "Suite de pruebas de carga y rendimiento para PharmaCRM — Hospital",
  "private": true,
  "scripts": {
    "test:smoke": "artillery run tests/basico.yaml --output resultados/smoke-report.json",
    "test:api": "artillery run tests/hospital-api.yaml --output resultados/api-report.json",
    "test:e2e": "artillery run tests/hospital-e2e.yaml --output resultados/e2e-report.json",
    "test:loyalty": "artillery run tests/hospital-loyalty.yaml --output resultados/loyalty-report.json",
    "test:jwt": "artillery run tests/jwt-auth.yaml --output resultados/jwt-report.json",
    "test:stress": "artillery run stress-5000.yml --output resultados/stress-report.json",
    "test:all": "npm run test:smoke && npm run test:api && npm run test:e2e && npm run test:loyalty && npm run test:jwt",
    "report": "artillery report resultados/smoke-report.json && artillery report resultados/api-report.json"
  },
  "devDependencies": {
    "artillery": "^2.0.31"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
